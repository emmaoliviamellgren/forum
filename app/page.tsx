import PublicLayout from './(root)/layout';
import LandingPage from './(root)/page';

const IndexPage = () => {
    return (
        <PublicLayout>
            <LandingPage />
        </PublicLayout>
    );
};

export default IndexPage;
